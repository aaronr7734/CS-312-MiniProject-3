<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AaronBlog - <%- title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
      }
      .post-content {
        font-size: 1.1rem;
      }
      .github-link {
        color: #0366d6;
        text-decoration: none;
        font-weight: bold;
        transition: color 0.3s ease;
      }
      .github-link:hover {
        color: #044289;
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <a href="#main-content" class="visually-hidden-focusable"
      >Skip to main content</a
    >

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Aaron's Blog</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if (user) { %>
            <li class="nav-item">
              <span class="navbar-text me-2">Welcome, <%= user.name %>!</span>
            </li>
            <li class="nav-item">
              <a href="/create-post" class="btn btn-primary me-2"
                >Create New Post</a
              >
            </li>
            <li class="nav-item">
              <a href="/signout" class="btn btn-outline-secondary">Sign Out</a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a href="/signin" class="btn btn-primary me-2">Sign In</a>
            </li>
            <li class="nav-item">
              <a href="/signup" class="btn btn-outline-secondary">Sign Up</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <header class="bg-primary text-white p-4">
      <h1>AaronBlog</h1>
      <p class="lead">A CS-312 project!</p>
    </header>

    <main id="main-content" class="container mt-4"><%- body %></main>

    <footer class="bg-light text-center p-3 mt-4">
      <a
        href="https://github.com/aaronr7734"
        class="github-link"
        target="_blank"
        rel="noopener noreferrer"
      >
        Visit my GitHub profile
      </a>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
